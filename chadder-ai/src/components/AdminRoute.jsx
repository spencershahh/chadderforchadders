import React from 'react';

// Absolutely minimal AdminRoute - no imports except React
const AdminRoute = ({ children }) => {
  // Directly return children with no checks
  return children;
};

export default AdminRoute; 